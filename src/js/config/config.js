export const config = {
  logging: true
}

export const defaultData = {
  options: {
    chatVisible: true,
    menuVisible: true,
    navVisible: true,
    startMuted: true,
    currentLayout: {id: 'grid', name: 'Grid'}
  },
  streamHistory: [],
  streamPresets: []
}
