export const config = {
  logging: true
}

export const defaultData = {
  options: {
    chatVisible: true,
    menuVisible: true,
    startMuted: true,
    currentLayout: { id: 'grid', name: 'Grid' }
  },
  streamHistory: [],
  streamPresets: [],
  streamFavorites: []
}
